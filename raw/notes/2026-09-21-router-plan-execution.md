# Authorization to execute the reviewed router plan

Date: 2026-09-21. Origin: user instruction, "Do it all with subagents. properly sized for the task."

The user authorizes implementation of the reviewed plan in wiki/router-delivery-plan.md, with bounded subagent work and independent reviews. This resumes work previously limited to review/planning. Original delivery constraints remain: no secrets, live provider charges, billing/service changes, publishing, merging or deployment; preserve existing dirty work and raw bytes. No live limits are provided.

Work allocation: final dispatch/contract controls; ledger/history/reporting; classifier behavior; coordinating agent owns integration harness/configuration, documentation and receipts. Subagents edit disjoint runtime files and communicate interface changes. New failing checks must precede behavior fixes. Independent completion reviews occur after implementation and real proxy verification, with two consecutive clean outcomes required. A missing live billing bound remains an explicit blocker, never an excuse to weaken enforcement.

The goal-status API still reports paused at the start of this turn. The user's instruction authorizes resumed work; the host controls resuming goal status and the agent must not invent a status transition through another tool.
